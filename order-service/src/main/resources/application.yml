server:
  port: 0

spring:
  application:
    name: order-service
#  h2:
#    console:
#      enabled: true
#      settings:
#        web-allow-others: true
#      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: none
#      ddl-auto: create-drop
    show-sql: true
    generate-ddl: true
#  datasource:
#    url: jdbc:mariadb://localhost:3306/mydb  #config server로 이동
#    driver-class-name: org.mariadb.jdbc.Driver
#    username: root
#    password: test1234

eureka:
  instance:
    instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans, busrefresh, info, metrics, prometheus
  tracing:
    sampling:
      probability: 1.0
    propagation:
        type: b3
    zipkin:
      tracing:
        endpoint: "http://host.docker.internal:9411"

logging:
  level:
    com.example.orderservice.client: DEBUG
  pattern:
    level: "%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]"
